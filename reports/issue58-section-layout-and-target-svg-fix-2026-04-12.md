# issue #58 図配置/10.8.3 修正メモ

## 背景
ユーザー指摘:
- 10.8.3/10.8.4 と 10.10.1/10.10.2 が離れており見づらい。
- 10.8.3 の 1行目と2行目の間に破線が入っており、出力結果との整合が悪い。
- 10.8.3 にヘッダーがない。

## 対応
1. 章配置の整理
- `Design/ExcelTemplate/ExcelTemplate_DetailDesign.md`
  - 10.8.8: C#データ展開後の挿入元（GroupBlockインスタンス）図を追加。
  - 10.8.9: C#データ展開後のInvoice出力図を追加。
  - 10.10.1 は「図の参照位置」説明へ変更し、10.8.8/10.8.9 を参照する構成へ変更。

2. 10.8.3 図の修正
- `Design/ExcelTemplate/assets/insert-target-cell-values.svg`
  - 破線（1行目と2行目境界として誤認される表現）を削除。
  - `A1` に `{{use:Header}}` を追加。
  - 中央 `{{use:GroupBlock, from:@groups, var:group}}` は維持。

3. 表の整合
- 10.8.1: `A1` に `{{use:Header}}` を追加。
- 10.8.5: `A1` のヘッダー挿入トリガ説明を追加。

## 結果
- 参照図が 10.8 節に集約され、入力図/展開後図を連続で確認可能。
- 10.8.3 の不要破線を除去し、ヘッダー挿入トリガを明示。
